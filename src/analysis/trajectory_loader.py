\"\"\"
Trajectory loading and file handling utilities.
\"\"\"\n\nimport os\nimport warnings\n\n\ndef load_trajectory(topology_file, trajectory_file):\n    \"\"\"\n    Load a molecular dynamics trajectory.\n    \n    Supports GROMACS (.xtc, .trr), AMBER (.dcd), and other MDAnalysis-compatible formats.\n    \n    Parameters\n    ----------\n    topology_file : str\n        Path to topology file (.gro, .pdb, .tpr, etc.)\n    trajectory_file : str\n        Path to trajectory file (.xtc, .dcd, .h5, etc.)\n    \n    Returns\n    -------\n    MDAnalysis.Universe\n        Loaded trajectory universe\n    \n    Raises\n    ------\n    FileNotFoundError\n        If files don't exist\n    ImportError\n        If MDAnalysis not installed\n    \n    Examples\n    --------\n    >>> u = load_trajectory(\"em.gro\", \"production.xtc\")\n    >>> print(len(u.trajectory))\n    1000\n    \"\"\"\n    try:\n        import MDAnalysis as mda\n    except ImportError:\n        raise ImportError(\"MDAnalysis not installed. Install with: conda install -c conda-forge mdanalysis\")\n    \n    if not os.path.exists(topology_file):\n        raise FileNotFoundError(f\"Topology file not found: {topology_file}\")\n    \n    if not os.path.exists(trajectory_file):\n        raise FileNotFoundError(f\"Trajectory file not found: {trajectory_file}\")\n    \n    u = mda.Universe(topology_file, trajectory_file)\n    return u\n\n\ndef validate_files(config):\n    \"\"\"\n    Validate that all trajectory files in config exist.\n    \n    Parameters\n    ----------\n    config : dict\n        Configuration dictionary with 'systems' key\n    \n    Returns\n    -------\n    bool\n        True if all files exist, False otherwise\n    \n    Examples\n    --------\n    >>> valid = validate_files(CONFIG)\n    >>> if valid:\n    ...     print(\"Ready to run analysis\")\n    \"\"\"\n    missing = []\n    \n    for sys_key, sys_info in config[\"systems\"].items():\n        topo = sys_info[\"topology\"]\n        traj = sys_info[\"trajectory\"]\n        \n        if not os.path.exists(topo):\n            missing.append(f\"{sys_key} topology: {topo}\")\n        \n        if not os.path.exists(traj):\n            missing.append(f\"{sys_key} trajectory: {traj}\")\n    \n    if missing:\n        print(\"⚠️ Missing files:\")\n        for msg in missing:\n            print(f\"  ❌ {msg}\")\n        return False\n    else:\n        print(f\"✅ All {len(config['systems'])} systems validated\")\n        return True\n\n\ndef get_trajectory_info(topology_file, trajectory_file):\n    \"\"\"\n    Get basic information about a trajectory.\n    \n    Parameters\n    ----------\n    topology_file : str\n        Path to topology file\n    trajectory_file : str\n        Path to trajectory file\n    \n    Returns\n    -------\n    dict\n        Dictionary with trajectory info (n_atoms, n_frames, duration, etc.)\n    \n    Examples\n    --------\n    >>> info = get_trajectory_info(\"em.gro\", \"production.xtc\")\n    >>> print(f\"Trajectory length: {info['duration_ns']:.1f} ns\")\n    \"\"\"\n    u = load_trajectory(topology_file, trajectory_file)\n    \n    # Get basic info\n    n_atoms = u.atoms.n_atoms\n    n_frames = len(u.trajectory)\n    dt_ps = u.trajectory.dt if hasattr(u.trajectory, 'dt') else 1.0\n    duration_ps = n_frames * dt_ps\n    duration_ns = duration_ps / 1000.0\n    \n    # Get protein info\n    protein = u.select_atoms(\"protein\")\n    n_residues = len(protein.residues)\n    \n    info = {\n        \"n_atoms\": n_atoms,\n        \"n_frames\": n_frames,\n        \"n_residues\": n_residues,\n        \"dt_ps\": dt_ps,\n        \"duration_ps\": duration_ps,\n        \"duration_ns\": duration_ns,\n        \"topology_file\": topology_file,\n        \"trajectory_file\": trajectory_file,\n    }\n    \n    return info\n"
