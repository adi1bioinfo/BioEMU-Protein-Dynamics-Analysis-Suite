name: Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip setuptools wheel
        # Install build dependencies manually to support --no-build-isolation
        python -m pip install versioneer Cython "numpy<2.0.0" oldest-supported-numpy
        # Install package without build isolation to force use of pre-installed numpy
        pip install --no-build-isolation -e .[dev]
        
    - name: Run tests
      run: |
        pytest
