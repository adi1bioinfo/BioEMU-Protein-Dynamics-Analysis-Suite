\"\"\"
Core analysis functions for MD trajectory processing.
\"\"\"\n\nimport numpy as np\nimport pandas as pd\nfrom scipy import spatial\n\n\ndef calculate_rmsd_rmsf(universe, atom_selection=\"protein and backbone\", reference_frame=0):\n    \"\"\"\n    Calculate RMSD and RMSF for a trajectory.\n    \n    Parameters\n    ----------\n    universe : MDAnalysis.Universe\n        Loaded trajectory\n    atom_selection : str\n        Atom selection string\n    reference_frame : int\n        Frame to use as reference (default: first frame)\n    \n    Returns\n    -------\n    tuple\n        (rmsd_array, rmsf_array)\n        - rmsd_array: (n_frames,) array of RMSD values\n        - rmsf_array: (n_atoms,) array of per-atom RMSF values\n    \n    Examples\n    --------\n    >>> u = load_trajectory(\"em.gro\", \"prod.xtc\")\n    >>> rmsd, rmsf = calculate_rmsd_rmsf(u)\n    >>> print(f\"Final RMSD: {rmsd[-1]:.2f} Å\")\n    \"\"\"\n    atoms = universe.select_atoms(atom_selection)\n    \n    # Get reference coordinates\n    universe.trajectory[reference_frame]\n    ref_coords = atoms.positions.copy()\n    \n    # Calculate RMSD for each frame\n    rmsd_array = []\n    for frame in universe.trajectory:\n        rmsd = np.sqrt(np.mean((atoms.positions - ref_coords)**2))\n        rmsd_array.append(rmsd)\n    \n    rmsd_array = np.array(rmsd_array)\n    \n    # Calculate RMSF\n    deviations = []\n    for frame in universe.trajectory:\n        deviations.append(atoms.positions - ref_coords)\n    \n    deviations = np.array(deviations)\n    rmsf_array = np.sqrt(np.mean(deviations**2, axis=0))\n    \n    return rmsd_array, rmsf_array\n\n\ndef calculate_sasa(universe, probe_radius=1.4, frame_stride=1):\n    \"\"\"\n    Calculate solvent-accessible surface area using MDTraj.\n    \n    Parameters\n    ----------\n    universe : MDAnalysis.Universe\n        Loaded trajectory\n    probe_radius : float\n        Probe radius in Ångströms\n    frame_stride : int\n        Process every nth frame\n    \n    Returns\n    -------\n    np.ndarray\n        SASA values (n_frames, n_residues)\n    \n    Notes\n    -----\n    Requires MDTraj to be installed.\n    \"\"\"\n    try:\n        import mdtraj as md\n    except ImportError:\n        raise ImportError(\"MDTraj required for SASA calculation. Install with: conda install -c conda-forge mdtraj\")\n    \n    # Convert trajectory\n    traj = md.Trajectory(\n        xyz=universe.trajectory.xyz[::frame_stride] * 0.1,  # Convert nm to Å\n        topology=md.Topology.from_openmm(universe.trajectory._topology) \n                 if hasattr(universe.trajectory, '_topology') \n                 else None\n    )\n    \n    # Calculate SASA\n    sasa = md.shrake_rupley(traj, mode='residue')\n    return sasa\n\n\ndef calculate_hydrogen_bonds(universe, distance_cutoff=3.5, angle_cutoff=120.0):\n    \"\"\"\n    Identify persistent hydrogen bonds.\n    \n    Parameters\n    ----------\n    universe : MDAnalysis.Universe\n        Loaded trajectory\n    distance_cutoff : float\n        D-A distance cutoff in Ångströms\n    angle_cutoff : float\n        D-H-A angle cutoff in degrees\n    \n    Returns\n    -------\n    dict\n        Dictionary with H-bond information\n    \n    Examples\n    --------\n    >>> hbonds = calculate_hydrogen_bonds(u)\n    >>> print(f\"Found {len(hbonds)} H-bond pairs\")\n    \"\"\"\n    \n    # Select donors and acceptors\n    donors = universe.select_atoms(\"name N or name O\")\n    acceptors = universe.select_atoms(\"name O or name N\")\n    \n    hbond_data = {}\n    \n    for frame in universe.trajectory:\n        # Calculate distances\n        distances = spatial.distance.cdist(\n            donors.positions,\n            acceptors.positions\n        )\n        \n        # Find H-bonds\n        hbonds = np.where(distances < distance_cutoff)\n        \n        for donor_idx, acc_idx in zip(hbonds[0], hbonds[1]):\n            donor_res = donors[donor_idx].resnum\n            acc_res = acceptors[acc_idx].resnum\n            pair = (donor_res, acc_res)\n            \n            if pair not in hbond_data:\n                hbond_data[pair] = 0\n            hbond_data[pair] += 1\n    \n    # Convert to occupancy\n    n_frames = len(universe.trajectory)\n    for pair in hbond_data:\n        hbond_data[pair] = hbond_data[pair] / n_frames\n    \n    return hbond_data\n\n\ndef calculate_contact_map(universe, selection1=\"protein\", selection2=\"protein\", \n                         distance_cutoff=4.5, frame_stride=1):\n    \"\"\"\n    Calculate contact map between two selections.\n    \n    Parameters\n    ----------\n    universe : MDAnalysis.Universe\n        Loaded trajectory\n    selection1 : str\n        First atom selection\n    selection2 : str\n        Second atom selection\n    distance_cutoff : float\n        Distance cutoff in Ångströms\n    frame_stride : int\n        Process every nth frame\n    \n    Returns\n    -------\n    np.ndarray\n        Contact map matrix (occupancy)\n    \"\"\"\n    atoms1 = universe.select_atoms(selection1)\n    atoms2 = universe.select_atoms(selection2)\n    \n    n_atoms1 = len(atoms1)\n    n_atoms2 = len(atoms2)\n    contact_count = np.zeros((n_atoms1, n_atoms2))\n    n_frames = 0\n    \n    for frame in universe.trajectory[::frame_stride]:\n        n_frames += 1\n        distances = spatial.distance.cdist(\n            atoms1.positions,\n            atoms2.positions\n        )\n        contact_count += (distances < distance_cutoff)\n    \n    # Normalize to occupancy\n    contact_map = contact_count / n_frames\n    \n    return contact_map\n"
